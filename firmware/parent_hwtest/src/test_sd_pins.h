/*
 * SD_MMC(CLK/CMD/D0)の配線を、SD_MMCライブラリを使わず生のGPIOとして
 * 電気的に検査する診断テスト。オシロ/テスタでのプローブが難しい場合の代替。
 *
 * 検査できること:
 *  - アイドル時(何も駆動しない状態)の読み値が安定しているか
 *    → 極端に安定していない(フローティング)か、外部プルアップ/プルダウンで
 *      安定してHIGH/LOWになっているかの目安になる
 *  - ESP32側パッドをHIGH/LOWに駆動し、その場で読み返して一致するか
 *    → GND/3.3Vへの直接短絡や、パッド自体の故障を検出できる
 *
 * 検査できないこと:
 *  - 断線(トレースが途中で切れている)は検出できない
 *    (ESP32側パッド単体でのループバック確認のため、SDソケット側までは見ていない)
 *
 * testSdMmc() より前に呼ぶこと (SD_MMCライブラリがピンを制御する前に検査するため)。
 */
#pragma once

void testSdPinIntegrity();
