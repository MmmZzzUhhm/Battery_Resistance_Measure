# (legacy) 単体デバイス向けPCデバッグツール

`iws7817_monitor.py` は旧アーキテクチャ (M5Stack AtomS3 単体 + UDP/HTTP、`firmware/_legacy_m5atoms3/`)
向けのデバッグ・可視化ツールです。

現在の子機/親機/ポータルサーバー構成 (ルートの `README.md` 参照) とは通信プロトコルが異なるため
**そのままでは動作しません**。単体のIWS7817モジュールをPCから直接デバッグしたい場合の参考として残しています。
